#include <stdio.h>
void sub(int a,int b)
{
    sub()
    
}
int main()
{
    int i;
    for(i=0;i<2;i++)
    return 0;
}
int sub(int a, int b)
{
    return (a - b);
}

int main()
{
    int a, b, c;
    printf("enter the number ");
    scanf("%d%d", &a, &b);
    c = sub(a, b);
    printf("subtract===%d",c);
    return 0;
    
}
